py -m venv .venv                          # Create Python virtual environment
.venv\Scripts\activate.bat                # Activate the Python virtual environment
pip install -r requirements.txt           # Install the necessary Python libraries in that virtual environment
py main.py                                # Actually run the Python script.

and then in ../AIMMSProject/data/ the file VS_5Vessel_15Solution.xlsx is just created.